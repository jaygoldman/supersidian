# Root folder where Supernote syncs notes.
# For Dropbox:
#   macOS + new Dropbox client often uses: ~/Library/CloudStorage/Dropbox/Supernote/Note
# For other setups, point this at the directory that contains your per-area folders
# (e.g. "Klick", "Personal", etc.)
SUPERSIDIAN_SUPERNOTE_ROOT=/Users/your-username/Library/CloudStorage/Dropbox/Supernote/Note

# Default place where Obsidian vaults live.
# You can still override per-bridge with an absolute vault_path.
SUPERSIDIAN_DEFAULT_OBSIDIAN_ROOT=/Users/your-username/Obsidian

# Path (relative to project root or absolute) to the JSON config file.
SUPERSIDIAN_CONFIG_PATH=./supersidian.config.json

# Optional: Explicit path to supernote-tool.
# If unset, Supersidian will fall back to looking for `supernote-tool` on PATH.
# Example:
# SUPERSIDIAN_SUPERNOTE_TOOL=/Users/your-username/Dev/supersidian/.venv/bin/supernote-tool
SUPERSIDIAN_SUPERNOTE_TOOL=

# Optional: location of the log file
# If unset, defaults to ~/supersidian.log
SUPERSIDIAN_LOG_PATH=/Users/jaygoldman/Dev/supersidian/supersidian.log

# Optional: default tags added to every note's frontmatter.
# Comma-separated, no spaces required.
SUPERSIDIAN_DEFAULT_TAGS=supernote

# Optional: Healthchecks.io URL for run monitoring.
# Leave empty to disable healthcheck pings.
# Example:
# SUPERSIDIAN_HEALTHCHECK_URL=https://hc-ping.com/your-uuid
SUPERSIDIAN_HEALTHCHECK_URL=

# Optional: Push notification endpoint
# This can be any HTTP endpoint that accepts JSON:
# - ntfy.sh
# - a Slack/Discord relay
# - IFTTT or Zapier webhook endpoints
# - a custom server or home‑automation rule
# NOTIFICATIONS can be all, errors, or none
SUPERSIDIAN_WEBHOOK_URL=
SUPERSIDIAN_WEBHOOK_TOPIC=
SUPERSIDIAN_WEBHOOK_NOTIFICATIONS=

# Optional: turn verbose logging on (1) or off (0)
SUPERSIDIAN_VERBOSE=0

# Optional: Task sync
# See README for instructions on obtaining an API token and adding support
# for other services
SUPERSIDIAN_TODO_PROVIDER=
SUPERSIDIAN_TODOIST_API_TOKEN=